# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Pre-existing project docs/assets outside the SvelteKit app scope (untouched
# by this scaffold; see repo root CLAUDE.md)
/SPEC.md
/MOCKUP.md
/DESIGN.md
/PLAN.md
/CLAUDE.md
/design-files/
/.claude/
/.superpowers/

# Generated output
/src/lib/paraglide/

# Pre-existing, hand-aligned design tokens ported in from the old top-level
# styles/_variables.scss (see repo root CLAUDE.md) rather than newly authored —
# left in its original alignment/quote style so `git log --follow` keeps
# tracing history through the Task 3 move. Also stylelint-ignored (see
# .stylelintrc.json); every other file under src/styles/ is fully linted.
/src/styles/_variables.scss
